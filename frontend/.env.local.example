# URL of the FastAPI backend used by the browser for direct calls (PDF upload)
# and by next.config.js rewrites for all /api/* requests.
# Only change this if your backend runs on a different port.
NEXT_PUBLIC_API_URL=http://localhost:8000